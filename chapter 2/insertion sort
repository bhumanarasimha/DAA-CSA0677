def insertion_sort(arr):
  n=len(arr)
  for i in range(1,n):
    key=arr[i]
    j=i-1
    while j>=0 and key<arr[j]
       arr[j+1]=arr[j]
       j=j-1
    arr[j+1]=key
  return arr
arr=[1,3,2,6,4,7,5,8,6,9,01]
print(insertion_sort(arr))
  
    
